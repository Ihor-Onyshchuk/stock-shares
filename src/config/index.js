export const dataSettings = {
  loading: false,
  error: false,
};

export const pageSettings = {
  page: 1,
  perPage: 10,
  isFirst: true,
  isLast: false,
}

export const initialState = {
  dataSettings,
  pageSettings,
  data: [],
  tableData: [],
};
